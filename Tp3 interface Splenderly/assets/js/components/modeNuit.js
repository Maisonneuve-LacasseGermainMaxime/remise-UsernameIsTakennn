// Variables

// Selections
const conteneurModeNuitHTML = document.querySelectorAll("nav li[data-page]");

// Fonction
// export function init() {
//     conteneurModeNuitHTML.forEach(function(mode){
//         mode.addEventListener("click", changerModeNuit);
//     })
    
//     changerModeNuit();
// }

// function changerModeNuit(evenement) {
        // cette variable me donne une erreur de target
//     const declencheur = evenement.currentTarget;
//     let boutonMode = declencheur.closest("li[data-page]");

//     if (boutonMode !== null) {
//         document.body.dataset.theme = boutonMode.dataset.mode;
//         localStorage.setItem("theme", boutonMode.dataset.mode);
//     }
// }

// execution
// init();
